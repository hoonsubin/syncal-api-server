// entities are used by TypeORM. It contains all the classes that are mapped to the database.
// this is different from using object models, which are only meant for this application.

export { default as User } from './User';
export { default as Event } from './Event';
